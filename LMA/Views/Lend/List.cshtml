@model IEnumerable<LMA.Data.Model.Book>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Authors name</th>
            <th>Lend a book</th>
        </tr>
    </thead>
    <tbody>
        @foreach (LMA.Data.Model.Book book in Model)
        {
            <tr>
                <td>@book.BookId</td>
                <td>@book.Name</td>
                <td>@book.Author.Name</td>
                <td><a class="btn btn-success" asp-area="" asp-controller="Lend" asp-action="Lend" asp-route-bookId="@book.BookId">Lend</a></td>
            </tr>
        }
    </tbody>
</table>
