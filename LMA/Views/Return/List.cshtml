@model IEnumerable<LMA.Data.Model.Book>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Authors name</th>
            <th>Borrower Name</th>
            <th>Return</th>
        </tr>
    </thead>
    <tbody>
        @foreach (LMA.Data.Model.Book book in Model)
        {
            <tr>
                <td>@book.BookId</td>
                <td>@book.Name</td>
                <td>@book.Author.Name</td>
                <td>@book.Borrower.Name</td>
                <td><a class="btn btn-success" asp-area="" asp-controller="Return" asp-action="ReturnBook" asp-route-bookId="@book.BookId">Return</a></td>
            </tr>
        }
    </tbody>
</table>
